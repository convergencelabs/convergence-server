version: 12
description: Fixes the name of the ChatChannelMember Channel-User Index
changes:

########## ChatChannelMember Channel-User Index Fix ##########

  - action: DropIndex
    name: ChatChannelUserState.channel_user

  - action: CreateIndex
    className: ChatChannelMember
    name: ChatChannelMember.channel_user
    type: UniqueHashIndex
    properties: [channel, user]

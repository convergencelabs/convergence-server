akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "DEBUG"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"

  actor {
    provider = "cluster"
  }

  remote {
    log-remote-lifecycle-events = off

    netty.tcp {
      hostname = "127.0.0.1"
      port = 0
    }
  }

  cluster {
    seed-nodes = [
      "akka.tcp://Convergence@127.0.0.1:2551"
    ]

    roles = []

    jmx.multi-mbeans-in-same-jvm = on
  }

  http.server.idle-timeout = 60 minutes
  http.server.remote-address-header = on
}

akka.cluster.jmx.multi-mbeans-in-same-jvm = on

convergence {

  domain-shutdown-delay = 10 minutes
  rest-domain-shutdown-delay = 10 minutes
  auth-token-expiration = 60 minutes

  domain-shutdown-delay = 10 minutes

  master-admin-rest-token = "token"

  orient-db = {
    db-uri = "remote:localhost:2424"
    rest-uri = "http://localhost:2480"
    admin-username = "root"
  	admin-password = "password"
  }

  convergence-database = {
    database = "convergence"

    admin-username = "admin"
    admin-password = "admin"

    username = "writer"
    password = "writer"

    auto-install = {
      enabled = true
      pre-release = false
    }

    retry-delay = 30s
  }

  domain-databases {
  	randomize-credentials = false
  	pre-release = true
  }

  auto-create-domains = [{
    id = "example"
    displayName = "Example Domain"
    anonymousAuth: true
  }]

  registration-base-url = "http://localhost:8081/registration/"
  admin-ui-url = "http://localhost:8081/admin/"

  protocol = {
    handshake-timeout = 5s
    default-request-timeout = 10s
    heartbeat = {
      enabled = false
      ping-interval = 5s
      pong-timeout = 10s
    }
  }

  websocket = {
    host = "0.0.0.0"
    port = 8080
    max-frames = 1024
    max-stream-duration = 10s
  }

  rest {
  	host = "0.0.0.0"
    port = 8081
    rest-domain-shutdown-delay = 10 minutes
    auth-token-expiration = 10 minutes
    max-rest-actor-shutdown = 30 seconds
  }

  smtp = {
  	username = "registration@convergencelabs.com"
    password = "7MBCC(aTu&~+"
    new-registration-to-address = "registration@convergencelabs.com"
    from-address = "registration@convergencelabs.com"
    host = "dallas113.arvixeshared.com"
    port = 587
  }

  zoho = {
    enabled = false
    enabled = ${?zoho_enabled}
    auth-token = ""
    auth-token = ${?zoho_auth_token}
  }
}
